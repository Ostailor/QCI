# Preserve checksum-pinned IEEE 123 upstream bytes exactly as downloaded.
data/upstream/ieee123/** -text -diff

# Python's CSV writer may emit CRLF; treat CR as part of the line ending.
*.csv whitespace=trailing-space,space-before-tab,cr-at-eol
